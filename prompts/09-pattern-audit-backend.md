Review the code in backend/calculator/ and backend/handlers/ and identify
which recognized design patterns it applies, even if not consciously
named while writing it. For each one: name the pattern, where exactly it
appears (file/function), and why it fits. Don't force a pattern that
isn't genuinely there — if nothing beyond the already-known CORS
Decorator (docs/decisions.md #14) is present, say so plainly rather than
stretching a label onto something that doesn't really fit.
