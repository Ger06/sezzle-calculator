Based on spec.md and plan.md, generate specs/001-calculator/tasks.md:
small tasks (max 15-20 min each, given the total assignment timebox is
2-4 hours), in dependency order, each with the RF(s) it covers and a
verifiable "Done when:" line. Use checkboxes.

Group tasks under two clear phases — Backend and Frontend — with backend
tasks completed and green before any frontend task starts.

Add a note at the top of tasks.md: if while executing a task something is
ambiguous or blocks progress (missing decision, unclear requirement), stop
and ask instead of guessing or working around it silently.
